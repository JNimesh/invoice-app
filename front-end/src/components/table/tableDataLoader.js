import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function TableDataLoader() {
  return (
    <Box sx={{ p: "20px" }}>
      <Typography>Data Loading ....</Typography>
    </Box>
  );
}
